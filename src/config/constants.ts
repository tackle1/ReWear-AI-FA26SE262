export const APP_CONSTANTS = {
  APP_NAME: 'Rewear AI',
  DEFAULT_PAGE_SIZE: 20,
  MAX_PAGE_SIZE: 100,
  MAX_UPLOAD_SIZE_BYTES: 10 * 1024 * 1024, // 10MB
  MAX_IMAGE_COUNT_PER_LISTING: 8,
  DEFAULT_TIMEOUT_MS: 30000,
} as const;
