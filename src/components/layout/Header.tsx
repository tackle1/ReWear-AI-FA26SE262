import React from 'react';

export const Header: React.FC = () => {
  return (
    <header className="app-header">
      <div className="logo">Rewear AI</div>
    </header>
  );
};

export default Header;
